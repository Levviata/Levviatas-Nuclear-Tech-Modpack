import net.minecraftforge.event.entity.EntityEvent
import com.hbm.entity.mob.glyphid.EntityGlyphid

@EventBusSubscriber(modid = "lntm")
public class Events {

      @SubscribeEvent
      public static void changeEntityOnHitSound(EntityEvent event) {
        if (event.getEntity == 
      }

    }